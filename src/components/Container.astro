---
import { cn } from "@/lib/utils";
import type { HTMLTag } from "astro/types";

interface Props {
  className?: string | undefined;
  tag?: HTMLTag;
}

const { className, tag: Element = "div" } = Astro.props as Props;
---

<Element class={cn("max-w-[1100px] mx-auto px-12 lg:px-6", className)}>
  <slot />
</Element>
